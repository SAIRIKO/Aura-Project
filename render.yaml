services:
  - type: web
    name: aura-backend
    runtime: node
    plan: free
    buildCommand: "cd backend && npm install && npm run build"
    startCommand: "cd backend && npm start"
    envVars:
      - key: NODE_ENV
        value: production
    envVarGroups:
      - aura-secrets
